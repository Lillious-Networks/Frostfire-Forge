services:
  frostfire-forge-dev:
    build:
      context: ../..
      dockerfile: src/docker/Dockerfile.dev
    container_name: frostfire-forge-dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_HOST: mysql-dev
      DATABASE_PORT: 3306
    volumes:
      - ../../.env.development:/app/.env.development:ro
    env_file:
      - ../../.env.development
    networks:
      - docker_gateway-network-dev

networks:
  docker_gateway-network-dev:
    external: true
